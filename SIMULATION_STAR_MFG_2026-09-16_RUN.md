# 星河智造模拟企业：真实流程验收记录

> 运行日期：2026-09-16  
> 运行环境：本机平台 `http://127.0.0.1:8012`  ̶  
> 数据性质：全部为本次生成的模拟数据，不含真实企业、员工、客户或订单信息。

## 1. 验收目的

按真实使用者的顺序，验证“没有现成信息系统的企业先建立企业投影，再接入一个已设定的 ERP 数据源，最后由管理 Agent 查询和分析”的完整链路：

```text
生成问卷/访谈材料
→ 创建公司和项目
→ 导入材料并建立企业投影
→ 查看、修改、发布投影
→ 登记只读 ERP 数据源
→ 导入 ERP 文件
→ 配置对象、字段、身份和关系映射
→ 物化统一数据
→ 导入工作观察
→ 查看岗位工作模式与差异
→ 用管理 Agent 查询、交叉分析和推演
→ 查看来源、状态和执行说明
```

## 2. 模拟企业和输入数据

模拟企业：**星河智造模拟有限公司**  
项目：**企业总体投影**  
ERP 数据源：**星河 ERP 模拟导出（只读）**

项目根目录保留了以下输入文件：

- `SIMULATION_STAR_MFG_2026-09-16_companycheck_answers.csv`：模拟问卷/CompanyCheck 导出
- `SIMULATION_STAR_MFG_2026-09-16_erp_customers.csv`：客户主数据
- `SIMULATION_STAR_MFG_2026-09-16_erp_orders.csv`：销售订单
- `SIMULATION_STAR_MFG_2026-09-16_erp_production.csv`：生产执行
- `SIMULATION_STAR_MFG_2026-09-16_erp_departments.csv`：部门
- `SIMULATION_STAR_MFG_2026-09-16_erp_roles.csv`：岗位

模拟数据包含订单延期、返工、质量待定、客户代码连接和岗位称谓差异，用于验证正常数据、异常数据、身份未解析和映射冲突场景。

## 3. 企业投影流程结果

材料导入和建模链路已跑通：

- 运行快照产生 **34 条证据片段** 和 **34 条结构化声明**；
- 企业投影草稿达到 **第 50 次修订**；
- 成功发布 **v1**；
- 正式投影包含 **22 个实体、17 条关系**；
- 实体类型包括 1 个应用、2 个业务对象、6 个组织单元、1 个流程、6 个流程步骤、6 个岗位；
- 关系类型包括 2 条指令流、6 条包含关系、3 条信息流、6 条执行关系。

发布后的投影只包含材料和人工确认过的正式内容。模型没有把管理观察、潜在假设或工作观察自动写成正式事实。

## 4. ERP 接入和语义对齐结果

### 4.1 数据源导入

已按“只读文件连接器”完成 5 类 ERP 数据导入：

| 数据对象 | 模拟记录数 |
| --- | ---: |
| 客户 | 4 |
| 销售订单 | 5 |
| 生产执行 | 5 |
| 部门 | 6 |
| 岗位 | 6 |

### 4.2 映射和物化

- 批准字段映射：**13 条**；
- 批准关系映射：**2 条**；
- 生产订单号 → 销售订单：建立 **5 条**关系；
- 部门负责岗位 → 岗位：建立 **6 条**关系；
- 销售订单批次物化：**37 条来源观测**；
- 生产执行批次物化：**35 条来源观测**；
- 统一业务对象数据集已可查询和导出，查询返回 2 条记录、0 条校验警告。

映射过程没有因为名称相似就直接合并对象。`生产计划员` 与 `计划员` 的差异被保留为需要确认的身份问题。

### 4.3 身份状态

已确认的示例绑定：

- `FIN` → 财务部；
- `sales` → 销售主管；
- `finance` → 财务主管。

本次复杂分析快照中仍有 **23 条来源记录身份未绑定**。因此 ERP 来源观测虽然已经读取和物化，但不会被冒充为正式企业事实；返回结果会标明 `identity_status=UNRESOLVED`、`trusted=false`，并显示映射状态和警告。

## 5. 工作观察流程结果

工作观察使用独立采集数据包模拟，不要求员工登录管理平台或填写流程：

- 模拟事件：**8 条**；
- 模拟员工：**2 名**；
- 模拟会话：**2 个**；
- 已生成岗位工作分析和路径统计。

观察到的路径示例：

```text
CRM 客户详情 → ERP 新建订单 → 表格报价核对 → CRM 客户详情
CRM 客户详情 → ERP 新建订单 → CRM 客户详情 → 邮件交付确认
```

相邻转换统计已生成，例如“CRM 客户详情 → ERP 新建订单”出现 2 次、涉及 2 名员工。前端对比功能也已实际执行，结果为“左侧独有 1 条，右侧独有 1 条”。

这些边只表示观察到的时间先后，不被系统表述为因果、绩效排名或最佳流程。

## 6. 推演流程结果

已使用“产能变化推演”验证分析 + Action 预览：

- 基准产能：每周期 100；需求：每周期 90；5 个周期；
- 基准结果：完成 450，期末积压 0；
- 事件场景产能：每周期 80；
- 事件结果：完成 400，期末积压 50。

当前 Action 只做参数校验、影响计算、执行计划和结果预览，不向 ERP、MES 或 CRM 写入。

## 7. 管理 Agent 真实运行

### 7.1 简单任务

已在浏览器中新建对话并提交精确查询：

- 查询订单 `SO-1002` 和 `SO-1005` 的状态、交付日期及质量状态；
- Agent 调用“按来源标识读取 ERP 观测”；
- 预览、执行、结果展示均成功；
- 返回 **6 条来源观测**；
- 结果包含来源资产、字段、实际值、身份状态、映射状态和可信度；
- 未进行推测或自动写入。

### 7.2 复杂任务

已提交固定范围的复杂任务：已发布投影 v1、全部 ERP 来源观测、8 条工作观察，要求识别至少两个待管理层核查的潜在问题，并区分正式事实、ERP 观测、工作观察和推测。

最终运行结果：

- 状态：**COMPLETED**；
- 工具动作：**6 个，全部 SUCCEEDED**；
- 实际读取：企业概览、ERP 来源观测、工作观察、管理交叉分析、管理观察、潜在记录；
- ERP 来源观测采用了程序强制补齐的宽范围只读读取，返回 **92 条当前来源观测**；
- 未确认身份的来源被明确列出；
- 没有执行任何外部系统写入；
- 潜在记录没有自动入库。

模型在最终总结阶段没有返回符合平台契约的结构化 JSON，因此系统使用已成功工具结果生成了透明的程序化摘要，没有用模型补写结论，也没有伪造潜在问题。这一降级结果已经显示在浏览器对话中。

## 8. 运行中发现并修复的问题

本次不是只调用接口，而是从浏览器实际操作后发现并处理了以下问题：

1. 外部模型权限勾选在前端不明显，修正了测试操作并确认模型调用状态；
2. 来源观测请求最初没有被任务路由识别，补充了来源观测任务识别；
3. ERP 业务字段没有完整物化，补齐来源字段到正式业务对象的物化链路；
4. 复杂任务的模型计划偶尔遗漏 ERP 读取，增加了程序级必需只读步骤；
5. 模型返回非法结构化内容时，改为透明程序摘要，不再伪造成功结论；
6. 第一次复杂运行超过本地上下文限制，系统记录了失败并允许继续；提高本地上限后续跑成功；
7. 浏览器文件选择器在自动化场景中不稳定，使用同一预览/确认契约完成了本机导入验证；
8. 一次身份绑定定位错误被发现并纠正，最终绑定状态以数据库和运行结果为准。

对话中保留了第一次超预算失败消息，这是有意保留的审计痕迹；其后的续跑消息已经显示最终完成。它不是重复执行造成的数据覆盖。

## 9. 当前仍需注意的限制

- 用户要求的模型别名 `deepseek-v4.1-flash` 被当前接口拒绝；接口支持的实际可用别名为 `deepseek-flash`，本次使用后者完成了连接验证和 Agent 规划。密钥只写入本机加密配置，没有写入代码、CSV、运行记录或回复。
- 来源身份仍有 23 条未绑定，所以这些来源观测只能用于管理分析，不能直接提升为正式事实。
- 复杂任务最终采用了程序化透明摘要，说明“工具执行可靠”已经成立，但“模型结构化总结”仍需继续优化提示词、上下文裁剪和契约重试。
- 本轮验证了 Action 预览和推演，不包含对真实 ERP/MES/CRM 的写入；行业执行动作仍需未来以独立连接器扩展。
- 目标定向测试、Ruff 检查和前端构建/类型检查通过；完整测试集曾受到系统临时目录权限限制，未将该环境问题伪装成测试通过。

## 10. 如何查看痕迹

1. 打开 `SIMULATION_STAR_MFG_2026-09-16_README.md` 查看模拟数据说明；
2. 直接打开本文件查看完整运行记录；
3. 在浏览器打开 `http://127.0.0.1:8012/executive/`；
4. 选择项目“企业总体投影”，进入管理 Agent；
5. 查看名为“管理决策 Agent”的模拟对话、工具动作、来源说明和最终摘要；
6. 在“企业数字投影”中查看 22 个实体、17 条关系和工作观察结果；
7. 在建设端查看 ERP 数据源、映射、冲突和统一数据集。

## 11. 2026-09-16 本机复核

后续复核时，浏览器自动化控制服务连续返回连接异常，无法重新接管当前浏览器窗口；这属于测试控制层故障，不是平台接口故障。通过本机 HTTP 请求复核结果如下：

| 地址 | 结果 |
| --- | --- |
| `/api/v3/health` | HTTP 200 |
| `/developer/` | HTTP 200 |
| `/executive/` | HTTP 200 |
| `/api/v3/openapi.json` | HTTP 200 |
| 项目 Agent 对话列表 | HTTP 200 |
| 最终复杂 Agent 运行记录 | HTTP 200，状态 COMPLETED |

因此当前本机平台仍在运行，之前已经完成并保留的浏览器流程记录仍然有效；待浏览器控制服务恢复后，可以继续做页面级回归操作。
