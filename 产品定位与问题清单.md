# 产品定位与问题清单

> 文档用途：团队 / 投资人一页对齐"我们做什么、不做什么"，并汇总截至 2026-08-24 的全部问题。
> 依据：通读 `src/enterprise_insight/` 264 个源文件 + 22 篇 docs + 6 篇 ADR + GitHub 同类项目调研（OpenFoundry / TypeDB / DataHub / GraphRAG / Presidio / Postgres RLS）。代码层面未改动任何文件。

---

## 一、产品定位

### 1. 一句话定位
**面向小企业的、以"治理闭环"为底座的企业数字镜像平台**：用 ERP 导出的结构化主干 + 规模化问卷捕获的散落知识，在"证据优先、AI 只提案、人做终审"的红线内，合成一份可追溯、可解释、可持续生长的企业模型。

### 2. 我们做什么 / 不做什么

| 做（V1 范围） | 不做（明确边界） |
|---|---|
| 证据哈希入库、不可变版本、零静默冲突 | 全局原始向量库、Agent 自由群聊 |
| 问卷/评估版本化摄入 + CSV 导入 | 自动执行 AI 生成的代码 |
| 语义映射（ERP → 对象类型归一化） | 早期微服务化 / 多系统全量对接 |
| 固定配方 Agent：产出关系/行动流程**提案** | 让 Agent 自主写入/执行（无闸门） |
| 人审后物化进数字镜像 | 跨企业 Hub 默认共享 |
| Edge↔Hub 签名加密离线交换 | 总部直连分部数据库 |

### 3. 目标客户与场景
- **客户**：小企业（系统少、隐性知识多），尤其**出海美国**的华人经营型企业。
- **切入点**：先以 **1 个 ERP 作底座**，用**问卷**把老板/老员工脑子里的客户关系、供应依赖、历史坑、经营判断"问"出来，结构化沉淀进镜像。

### 4. 核心差异化（真做对的）
证据优先、零静默冲突、不可变版本、签名加密交换、7 角色 RBAC —— 这些是"可信"的**可验收红线**，多数开源项目都未必具备。

### 5. 与 Palantir 的关系
**借鉴其"本体 + 受治理动作"的思路，但不盲目对标**。PLTR 卖"本体 + 可执行受治理动作 + 数据操作系统"；我们当前是"证据底座 + 受控 LLM 提案器 + 离线签名交换"。差距集中在"数字镜像（本体）"与"Agent 真执行"两块，按小企业体量逐步补齐。

---

## 二、核心诊断：根因

**最核心问题 = 没有构造对现实企业的"数字镜像"（Digital Twin / Ontology）。**

之前的 P0 三条（无本体引擎、无实体关系、Agent 只提案不执行）是**同一空缺的三种表现**——缺一层把现实企业映射成"可查询 · 可组合 · 可驱动"模型的本体层。它也是 Flywheel/Eval 只能做成临时内联、语义图连不成网的根本原因。

数字镜像的原始材料（用户定调）：
- **企业内部系统**（小企业视角 = 1 个 ERP，作结构化主干）
- **系统外数据**（暂降级，离线包留以后用）
- **老员工经验**（散落知识，靠问卷 + Agent 提案捕获）

---

## 三、问题清单（分级汇总）

### P0 · 代差级（决定产品定位，非修修补补能解）
1. **无数字镜像 / 本体引擎**：只有扁平事实库，缺对象/链接/动作类型与图查询，数据不可组合、Agent 无法"本体感知"。→ 根因。
2. **无实体↔实体关系**：`Fact` 是「实体—谓词—标量值」的属性断言，系统整体没有 link/边类型，"语义图"连不起来。
3. **Agent 只提案不执行**：5 个固定配方只产 `PROPOSED` 文本/映射候选，不建关系、不建行动、不运行，缺"AI 真办事"闭环。

### P1 · 工程半成品（可逐项补齐）
4. **系统接入缺失**：仅问卷/证据文本 + 手工 Fact；无 ERP connector / 转换管道 / 血缘。小企业策略下收敛为"需 1 个 ERP connector + 归一化映射"（semantics 映射层可承担）。
5. **飞轮每请求全量重建内存注册表**（O(总贡献数)），无 SQL 持久化，RLock 形同虚设。
6. **评估无持久化、无管理 API**：基线/黄金集由请求体内联传入，`InMemoryEvaluationRegistry` 从未被路由接线。
7. **reliability 死代码**：`Outbox` 写事件无消费者、`Idempotency` 从未实例化、无后台 Worker、全程同步阻塞。
8. **多租户仅应用层 `tenant_id`**：无 DB 级 RLS，漏写一处 join 即可能跨租户泄露（与第一条红线冲突）。
9. **RBAC 粗、无 ABAC**：只有角色→权限，无属性级/资源归属策略。
10. **DLP 自报布尔**：发布门禁信任前端自填标志，非真实 PII 扫描（Presidio 可替）。
11. **无向量/语义检索**："语义"能力天花板低（GraphRAG/pgvector 可补，默认关、按 workspace 隔离）。
12. **无数据血缘/转换管道**：仅"证据撤销→依赖追踪"窄血缘，无管道级 lineage（Foundry/DataHub 式）。

### P2 · 产品 / 治理
13. **命名高度复刻 Palantir**：以美国为目标市场存在商标风险，建议品牌层切分（包名 `enterprise-insight-platform` 已中性）。
14. **文档领先实现**：飞轮持久性、Eval 管理、Outbox 投递、DLP 检测等多处承诺未落地，易误判成熟度。
15. **扩展沙箱 fail-closed**：只注册/规划策略、永不执行第三方代码，扩展性卖点暂虚（V1 可接受）。

---

## 四、对标与借鉴（来源）

- **OpenFoundry / syzygyhack / qintianhaohao**：ontology ODL（object/link/action）+ Cedar/OpenFGA ABAC + 受治理 Action（有真实副作用）。
- **TypeDB**：强类型 schema，确定性导出供 Agent 生成查询。
- **DataHub / Marquez**：统一元数据图 + 列级血缘 + Kafka 流式。
- **Microsoft GraphRAG**：实体关系图 + 社区摘要 + 检索（补"语义"）。
- **Presidio**：真实 PII 检测/脱敏（替 DLP 自报）。
- **Postgres RLS**：`SET LOCAL app.current_tenant` + `FORCE RLS`（补零跨租户）。

---

## 五、建议优先级（按 ROI）

1. **数字镜像最小闭环**：加 `Link` 类型 → Agent `ONTOLOGY`/`DIAGNOSIS` 产出 `RELATIONSHIP_PROPOSAL` → 人审物化进语义图（撬动 P0-2/3，风险最低）。
2. **ERP connector + 归一化映射**（承接小企业策略）。
3. **Postgres RLS + ABAC**（补零跨租户红线）。
4. **受治理 Action Framework**（声明式动作 + 前置条件 + 审计 + 人工闸门）。
5. **Flywheel/Eval 改 SQL 持久化 + 后台 Worker**（删两个半成品）。
6. **Presidio 替 DLP 自报 + pgvector（默认关）**。
7. **品牌/商标切分 + 补 DLP 真实扫描**。

---

*生成时间：2026-08-24 ｜ 状态：分析文档，未改动项目代码。*
