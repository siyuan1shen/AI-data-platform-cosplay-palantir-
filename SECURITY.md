# Security Policy

## Supported version

当前只支持仓库默认分支上的最新 V1。项目仍处于本地优先阶段，不应未经额外加固直接暴露到公网。

## Reporting a vulnerability

请通过仓库维护者提供的私密安全报告渠道提交漏洞；在维护者公布渠道前，不要把漏洞细节、真实密钥、企业数据或可利用样例发布到公开 Issue。报告至少包含受影响版本、复现步骤、影响范围和经过脱敏的证明。

## Sensitive data rules

- 永远不要上传真实企业数据库、问卷回答、Evidence、日志或备份；
- 永远不要发送 API Key、密码、访问令牌或主密钥；
- 证明密钥泄漏问题时使用明确标注的无效测试值；
- 发现真实秘密后先撤销/轮换，再清理 Git 历史；仅删除工作树文件并不足够；
- 默认部署只绑定 `127.0.0.1`，公网部署需要 OIDC、TLS、反向代理、速率限制、PostgreSQL/RLS、托管 Vault、对象存储扫描和独立安全评审。

## Trust boundaries

AI 输出只是 Proposal；扩展包只登记不执行；跨企业资产只有通过授权、DLP 和独立人工审核后才能发布。任何绕过这些边界的行为都按安全漏洞处理。
